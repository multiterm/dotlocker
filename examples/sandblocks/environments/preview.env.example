NODE_ENV=production
HOST=0.0.0.0
PORT=3000
PLUTO_DATA_DIR=/data
# Abby's existing Pluto PostgreSQL cluster is available over the tailnet.
# Use the dedicated preview role/password stored on Abby; never copy it here.
PLUTO_DATABASE_URL=postgres://pluto_preview:replace-me@100.114.99.85:54329/pluto_preview
