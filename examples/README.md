# Pluto examples

[`file-sync/run.sh`](./file-sync/run.sh) demonstrates a token-authenticated push/pull round trip. Set `PLUTO_TOKEN`, `PLUTO_ORG`, and `PLUTO_REPO`, then run it from a directory containing the files to synchronize.

```sh
sh examples/file-sync/run.sh
```
